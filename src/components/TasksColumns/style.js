import { makeStyles } from '@material-ui/core'

export default makeStyles(() => ({
  root: {
    display: 'flex',
    backgroundColor: 'red',
    padding: '10px 0',
    minHeight: '80vh',
    overflow: 'auto',
  },
}))
